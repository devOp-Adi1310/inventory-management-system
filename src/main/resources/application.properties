# MySQL Database Connection
# If a cloud environment variable exists, use it. Otherwise, default to your local database!
spring.datasource.url=${DB_URL:jdbc:mysql://localhost:3306/inventory_db?allowPublicKeyRetrieval=true&useSSL=false}
spring.datasource.username=${DB_USER:root}
spring.datasource.password=${DB_PASS:Devil6295158073}

# Hibernate Settings (Automatically creates/updates your tables)
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect